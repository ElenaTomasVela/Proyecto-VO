# Trabajo DeepCell

## Cómo ejecutar el proyecto

Asegúrate de tener Docker instalado. Con ello, abre en una terminal de comandos
en el directorio de este proyecto y usa la configuración de Docker Compose
incluida:

```bash
docker compose up -d
```
